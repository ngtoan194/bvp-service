spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://123.24.206.9:3306/bvp_upgrade?useUnicode=yes&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=Isora@123
spring.datasource.hikari.connection-init-sql=SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci
server.port=8983
#spring.datasource.hikari.connection-init-sql=SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci;

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=50MB
# Max Request Size
spring.servlet.multipart.max-request-size=75MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
project.data=/mnt/apps/bvp_upgrade
file.upload-dir=${project.data}/new_files
image.upload-dir=${project.data}/new_images

#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
